export async function checkThisUserIfTheyCanVoteAgain(userinput) {
    //select * WHERE userid = userinput.id

    //check count is at least 1

    //then, check for no votes in last 12 hrs
    // then, check no vote reminder was sent since last vote 
    //check has not opted out

   
    //send vote reminder
            //when they opt out... trigger opt out msg function
     //vote reminder sent

     //save that vote reminder was made
}

//new function
        //on react
        //lookup if this is a vote notification
            //look up if they opted out
                //run opt out msg function